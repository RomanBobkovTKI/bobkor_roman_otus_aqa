1. Как правильно сделать

host = request.config.getoption("--url")
и выбор через env

я сделал так:

host = request.config.getoption("--url")

    if host == "default":
        host = os.getenv("LOCALHOST")

    url = f"{protocol}://{host}:{port}"

2. Вообще кто-нибудь пишет на селениуме чистом?
Я слышал что селениум "не очень удобен", люди пользуются selenoid'om
Конечно для курса я считаю полезно знать основы и полезно уметь понимать как оно работает внутри, но все же интересно.

3.Как структурировать тесты? Например у меня есть test_main_page
один файл, где у меня проверка видимости элементов, проверка текстов элементов, наличие плейсхолдеров и тд.

А переходы по ссылкам например. Не лучше ли сделать директорию, которую разделить на файлики:
- main_page(dir)
-- test_visible_element
-- link_url

Плюс получается очень много кейсов и файл с тестами очень большой, это ок?

4. Как писать тесты? Интересует сам код. Есть какой-то подход к написанию тестов? бест практисы. Когда я пишу, у меня складывается
ощущение, что я пишу какие-то не консистентные тесты. Как это исправить и чего придерживаться, чтобы избежать такого

5. Вообще стоит ли делать проверку на отображение?

имею в виду

element.is_displayed()


6. Как корректней проверять клик?

по элементу в котором ссылка? или по ссылке?

<div><a href="qwe">qwe</a></div>

7. Вот тут нормально ли проверять что страница прогрузилась через
wait_element чтобы корректно определить driver.current_url

@pytest.mark.main_page
def test_click_main_logo(driver, presta_shop_url):
    driver.get(presta_shop_url)

    logo = wait_element("#_desktop_logo a", driver)
    logo.click()

    wait_element("#_desktop_logo a", driver)

    assert driver.current_url == f"{presta_shop_url}/", f"Неверная ссылка по лого"


 8. Почему когда я запускаю --headless у меня часто падают тесты с ошибкой:
 AssertionError: Не дождался видимости элемент

 9. Когда мы делаем проверку типа:
 assert header_text == "CLOTHES"
 может стоит приводить все к единому регистру? Или для теста важен и регистр?


10. Как делать выбор урла, по которому ходим сюда?
чат гпт подсказал несколько вариантов, я выбрал:

path = getattr(request, "param", None)

и в тесте:

@pytest.mark.parametrize("presta_shop_url", ["3-clothes"], indirect=True)


11. Как лучше переходить на определенную страницу? Сразу по адресу?(передачей пути, например /category) или кликаньем?

 КОММЕНТЫ К ДЗ:
 1. В main_page покрыл много кейсов хедер сайта, который есть на всех страницах, По хорошему нужно вынести в отдельную проверку хедера?
 А в main_page применять то что относится к главной?