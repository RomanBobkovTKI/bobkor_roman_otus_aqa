# Log Parser

Скрипт для анализа access.log файлов.

## Возможности

- Поддержка одного файла или директории
- Подсчет:
  - общего числа запросов
  - HTTP-методов
  - топ-3 IP
  - топ-3 самых долгих запросов
- Сохранение результатов в JSON

## Запуск

```bash
python3 log_parser.py access.log